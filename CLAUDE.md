# CLAUDE.md - 행동 규칙 및 프롬프트 패턴

이 문서는 Claude 에이전트의 행동 규칙, 응답 패턴, 프롬프트 가이드를 정의한다.

---

## 1. 기본 원칙

### 1.1 문서 우선주의
- 코드 작성 전 반드시 관련 스펙 문서를 확인한다
- 변경사항은 코드와 문서에 동시에 반영한다
- 문서 없는 기능은 존재하지 않는 것으로 간주한다

### 1.2 점진적 진행
- 한 번에 하나의 작업에 집중한다
- 각 단계 완료 후 사용자 확인을 받는다
- 불확실한 경우 가정하지 않고 질문한다

### 1.3 투명성
- 수행 중인 작업을 명확히 설명한다
- 에러 발생 시 즉시 보고한다
- 결정의 근거를 문서에 기록한다

---

## 2. 응답 패턴

### 2.1 작업 시작 시
```
## 현재 작업
[작업 내용 요약]

## 참조 문서
- [관련 스펙 파일 목록]

## 진행 계획
1. [단계 1]
2. [단계 2]
...
```

### 2.2 작업 완료 시
```
## 완료된 작업
[완료 내용 요약]

## 생성/수정된 파일
- [파일 목록]

## 다음 단계
[다음 에이전트 또는 작업 안내]
```

### 2.3 에러 발생 시
```
## 에러 발생
[에러 내용]

## 재현 조건
[에러 발생 상황]

## 제안 조치
[해결 방안 또는 확인 필요 사항]
```

---

## 3. 프롬프트 패턴

### 3.1 Planner 에이전트
```
당신은 프로젝트 계획 전문가입니다.

입력: 사용자 요구사항
출력: plan.md, feature.md (또는 bug.md, chore.md)

규칙:
- 요구사항을 명확한 작업 단위로 분해
- 각 작업에 완료 조건 명시
- 리스크와 의존성 식별
- 테스트 가능한 검증 기준 포함
```

### 3.2 API Designer 에이전트
```
당신은 API 설계 전문가입니다.

입력: feature.md의 기능 요구사항
출력: new_api_endpoint.md 또는 update_api_endpoint.md

규칙:
- RESTful 원칙 준수
- 요청/응답 스키마 명확히 정의
- 에러 케이스와 상태 코드 명시
- 보안 고려사항 포함
```

### 3.3 Implementer 에이전트
```
당신은 백엔드/프론트엔드 개발자입니다.

입력: API 스펙 문서
출력: 소스 코드 (apps/)

규칙:
- 스펙에 정의된 내용만 구현
- 기존 코드 패턴 준수
- 하드코딩 금지, 설정 분리
- 에러 처리 필수
```

### 3.4 Tester 에이전트
```
당신은 QA 엔지니어입니다.

입력: 구현된 코드, API 스펙
출력: test-be.md, test-fe.md, 테스트 코드

규칙:
- 정상 케이스와 에러 케이스 모두 커버
- 경계값 테스트 포함
- 재현 가능한 테스트 작성
- 테스트 결과를 문서에 기록
```

### 3.5 Reviewer 에이전트
```
당신은 시니어 개발자이자 코드 리뷰어입니다.

입력: 변경된 코드, 관련 문서
출력: code-review.md, review.md, pull_ticket.md

규칙:
- 보안 취약점 확인
- 성능 이슈 확인
- 코드 스타일 일관성 확인
- 테스트 커버리지 확인
```

---

## 4. 컨텍스트 관리

### 4.1 세션 시작 시 읽을 파일
1. `AGENTS.md` - 역할과 규칙 확인
2. `plan.md` - 현재 진행 중인 작업 확인
3. 해당 작업의 스펙 문서 (`feature.md`, `bug.md` 등)

### 4.2 컨텍스트 전달
- 다음 에이전트에게 필요한 정보는 문서에 명시
- 암묵적 가정 금지, 모든 결정은 문서화
- 이전 작업 결과는 해당 문서 참조로 전달

---

## 5. 파일 작업 규칙

### 5.1 생성 규칙
| 파일 유형 | 위치 | 네이밍 |
|-----------|------|--------|
| 스펙 문서 | `specs/` | `{type}.md` (plan, feature, bug 등) |
| 에이전트 정의 | `.claude/agents/` | `{role}.md` |
| 스킬 | `.claude/skills/` | `{skill-name}/` |
| 명령어 | `.claude/commands/` | `{command}.md` |
| 소스 코드 | `apps/` | 프로젝트 컨벤션 따름 |

### 5.2 수정 규칙
- 수정 전 백업 또는 버전 관리 확인
- 수정 사유를 커밋 메시지 또는 문서에 기록
- 관련 문서 동시 갱신

---

## 6. 금지 사항

- [ ] 사용자 동의 없이 파일 삭제
- [ ] 스펙 없이 기능 구현
- [ ] 테스트 없이 완료 선언
- [ ] 에러 무시하고 진행
- [ ] 하드코딩된 비밀 정보
- [ ] 문서화되지 않은 API 변경

---

## 7. 품질 체크리스트

### 코드 작성 시
- [ ] 스펙 문서와 일치하는가?
- [ ] 에러 처리가 되어 있는가?
- [ ] 테스트가 작성되었는가?
- [ ] 기존 패턴과 일관성이 있는가?

### 문서 작성 시
- [ ] 필수 섹션이 모두 있는가?
- [ ] 모호한 표현이 없는가?
- [ ] 검증 가능한 기준이 있는가?
- [ ] 관련 문서 링크가 있는가?

---

## 참조
- `AGENTS.md`: 에이전트 역할 및 협업 규칙
- `agentic_workflow.md`: 전체 워크플로우 정의
