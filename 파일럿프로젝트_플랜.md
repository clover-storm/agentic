# 파일럿 프로젝트 플랜: 에이전틱 레이어 구축

## 1. 프로젝트 개요

**프로젝트명**: Agentic Layer Pilot
**목적**: 에이전틱 인프라를 직접 구축하여 자동화와 생산성 향상을 체험하고, 실제 프로젝트에 적용할 수 있는 역량 확보

### 핵심 목표
- 앱 구축이 아닌 **에이전틱 레이어 자체의 설계와 구현**에 집중
- 에이전트, 스킬, 명령어를 통한 **반복 작업 자동화** 체험
- 문서 기반 워크플로우로 **일관성 있는 프로세스** 확립
- 간단한 앱은 에이전틱 레이어를 **검증하기 위한 테스트 대상**으로만 활용

---

## 2. 에이전틱 레이어란?

```
┌─────────────────────────────────────────────────────────┐
│                    에이전틱 레이어                        │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐        │
│  │   Agents    │ │   Skills    │ │  Commands   │        │
│  │ (역할 정의)  │ │ (자동화 단위)│ │ (CLI 루틴)  │        │
│  └─────────────┘ └─────────────┘ └─────────────┘        │
│  ┌─────────────────────────────────────────────┐        │
│  │  AGENTS.md / CLAUDE.md / mcp.json           │        │
│  │  (행동 규칙, 제약, 외부 도구 연결)            │        │
│  └─────────────────────────────────────────────┘        │
│  ┌─────────────────────────────────────────────┐        │
│  │  specs/ (plan, feature, bug, test, review)  │        │
│  │  (문서 기반 프로세스)                         │        │
│  └─────────────────────────────────────────────┘        │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
              ┌─────────────────────┐
              │   앱 레이어 (대상)   │
              │  (테스트용 간단 앱)  │
              └─────────────────────┘
```

---

## 3. 구축할 에이전트 및 스킬

### 3.1 에이전트 (역할별 전문가)

| 에이전트 | 역할 | 자동화 효과 |
|----------|------|-------------|
| `planner` | 요구사항 → plan.md 자동 생성 | 계획 수립 시간 단축 |
| `api-designer` | API 스펙 문서 자동 생성 | 일관된 API 문서화 |
| `implementer` | 스펙 기반 코드 스캐폴딩 | 보일러플레이트 제거 |
| `tester` | 테스트 케이스 자동 생성 | 테스트 커버리지 확보 |
| `reviewer` | 코드 리뷰 체크리스트 자동화 | 리뷰 품질 일관성 |
| `documenter` | 변경사항 → 문서 자동 갱신 | 문서 동기화 유지 |

### 3.2 스킬 (재사용 가능한 자동화 단위)

| 스킬 | 기능 | 트리거 |
|------|------|--------|
| `generate-plan` | 요구사항 입력 → plan.md 템플릿 채우기 | 새 작업 시작 시 |
| `generate-api-spec` | 기능 정의 → new_api_endpoint.md 생성 | API 설계 시 |
| `scaffold-endpoint` | API 스펙 → 코드 뼈대 생성 | 구현 시작 시 |
| `generate-tests` | 코드/스펙 → 테스트 케이스 생성 | 구현 완료 후 |
| `run-review` | 변경 코드 → review.md 자동 작성 | PR 전 |
| `sync-docs` | 코드 변경 → 관련 문서 갱신 | 커밋 후 |

### 3.3 명령어 (자주 쓰는 워크플로우)

| 명령어 | 동작 |
|--------|------|
| `/new-feature` | feature.md 생성 + plan.md 갱신 |
| `/new-api` | new_api_endpoint.md 생성 + 스캐폴드 |
| `/run-tests` | 테스트 실행 + test-*.md 결과 기록 |
| `/pre-pr` | 리뷰 체크리스트 + pull_ticket.md 생성 |
| `/start` | 로컬 환경 구동 (start-apps.md 기반) |

---

## 4. 자동화로 얻는 생산성 향상

### Before (수동)
```
요구사항 접수 → 문서 직접 작성 → 코드 작성 → 테스트 직접 작성
→ 리뷰 요청 → PR 작성 → 문서 수동 갱신
```
**문제점**: 반복 작업, 문서 누락, 형식 불일치, 컨텍스트 전환 비용

### After (에이전틱 레이어)
```
요구사항 입력 → [planner 에이전트] → plan.md + feature.md 자동 생성
              → [api-designer] → API 스펙 자동 생성
              → [implementer] → 코드 스캐폴드 자동 생성
              → [tester] → 테스트 케이스 자동 생성
              → [reviewer] → 리뷰 + PR 문서 자동 생성
```
**효과**:
- 반복 작업 80% 감소
- 문서-코드 동기화 자동 유지
- 일관된 품질과 형식
- 에이전트가 컨텍스트를 유지하므로 전환 비용 제거

---

## 5. 단계별 구축 계획

### Phase 1: 기반 구조 생성 ✅
**목표**: 에이전틱 레이어의 뼈대 구축

- [x] 디렉터리 구조 생성
  ```
  .claude/
  ├── agents/
  ├── skills/
  └── commands/
  specs/
  ai_docs/
  ```
- [x] `AGENTS.md` 작성: 에이전트 역할, 협업 규칙, 제약사항
- [x] `CLAUDE.md` 작성: 행동 규칙, 프롬프트 패턴
- [x] `mcp.json` 설정: 파일 시스템, 터미널 접근 정의

### Phase 2: 핵심 에이전트 정의 ✅
**목표**: 워크플로우 자동화의 주체 정의

- [x] `.claude/agents/planner.md` - 계획 수립 에이전트
- [x] `.claude/agents/api-designer.md` - API 설계 에이전트
- [x] `.claude/agents/implementer.md` - 구현 에이전트
- [x] `.claude/agents/tester.md` - 테스트 에이전트
- [x] `.claude/agents/reviewer.md` - 리뷰 에이전트

### Phase 3: 스킬 패키지 구현 ✅
**목표**: 재사용 가능한 자동화 단위 구축

- [x] `.claude/skills/generate-plan/` - plan.md 자동 생성
- [x] `.claude/skills/generate-api-spec/` - API 스펙 생성
- [x] `.claude/skills/scaffold-endpoint/` - 코드 스캐폴딩
- [x] `.claude/skills/generate-tests/` - 테스트 생성
- [x] `.claude/skills/sync-docs/` - 문서 동기화

### Phase 4: 명령어 및 워크플로우 연결 ✅
**목표**: 일상 업무에서 바로 사용할 수 있는 CLI 명령

- [x] `.claude/commands/new-feature.md`
- [x] `.claude/commands/new-api.md`
- [x] `.claude/commands/run-tests.md`
- [x] `.claude/commands/pre-pr.md`

### Phase 5: 검증 (간단한 앱으로 테스트) ✅
**목표**: 구축한 에이전틱 레이어가 실제로 동작하는지 검증

- [x] 간단한 TODO API를 **에이전틱 레이어를 통해** 구현
  - `/new-feature "Task CRUD"` 실행 → 문서 자동 생성 확인
  - `/new-api` 실행 → API 스펙 자동 생성 확인
  - 구현 후 `/run-tests` → 테스트 자동 생성 확인
  - `/pre-pr` → 리뷰 문서 자동 생성 확인
- [x] 자동화 효과 측정 및 개선점 도출

### Phase 6: 회고 및 실제 프로젝트 적용 준비 ✅
**목표**: 학습 내용 정리, 실전 적용 계획

- [x] `document.md`에 의사결정 및 학습 내용 기록
- [x] 실제 프로젝트에 적용할 에이전트/스킬 우선순위 결정
- [x] 커스터마이징 포인트 문서화

---

## 6. 디렉터리 구조 (최종 예상)

```
agentic/
├── .claude/
│   ├── agents/
│   │   ├── planner.md
│   │   ├── api-designer.md
│   │   ├── implementer.md
│   │   ├── tester.md
│   │   └── reviewer.md
│   ├── skills/
│   │   ├── generate-plan/
│   │   ├── generate-api-spec/
│   │   ├── scaffold-endpoint/
│   │   ├── generate-tests/
│   │   └── sync-docs/
│   └── commands/
│       ├── new-feature.md
│       ├── new-api.md
│       ├── run-tests.md
│       └── pre-pr.md
├── specs/
│   ├── plan.md
│   ├── feature.md
│   ├── bug.md
│   ├── chore.md
│   ├── new_api_endpoint.md
│   ├── test-be.md
│   ├── test-fe.md
│   ├── review.md
│   ├── code-review.md
│   ├── pull_ticket.md
│   └── start-apps.md
├── ai_docs/
│   └── (에이전트 참조 지식)
├── apps/
│   └── (검증용 간단 앱)
├── AGENTS.md
├── CLAUDE.md
├── mcp.json
└── document.md
```

---

## 7. 성공 기준

| 항목 | 기준 |
|------|------|
| 에이전트 정의 | 5개 에이전트 역할 명확히 정의 |
| 스킬 구현 | 최소 3개 스킬이 실제로 동작 |
| 자동화 체험 | 명령어 실행 → 문서 자동 생성 확인 |
| 워크플로우 완주 | 요구사항 → PR까지 에이전틱 레이어 통해 진행 |
| 적용 가능성 | 실제 프로젝트에 적용할 에이전트/스킬 3개 이상 선정 |

---

## 8. 기대 효과

1. **자동화 이해**: 어떤 작업이 자동화 가능한지 체감
2. **일관성 확보**: 문서 형식, 코드 패턴의 표준화
3. **컨텍스트 유지**: 에이전트가 프로젝트 맥락을 기억
4. **확장성**: 새로운 스킬/에이전트 추가로 자동화 범위 확대
5. **실전 적용 역량**: 실제 프로젝트에 바로 적용 가능한 템플릿 확보

---

## 9. 다음 단계

승인 시 Phase 1 (기반 구조 생성)부터 시작:
1. 디렉터리 구조 생성
2. `AGENTS.md` 초안 작성
3. `CLAUDE.md` 초안 작성

---

## 참조
- `agentic_workflow.md`: 워크플로우 기준 문서
